services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - ./data:/app/data

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:8899:80"
    depends_on:
      - backend
